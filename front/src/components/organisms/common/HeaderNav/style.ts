import styled from 'styled-components';
import tw from 'twin.macro';

export const StyledHeaderNav = styled.div`
  ${tw`fixed w-screen bg-blue-200`}
  height: 200px;
`;
StyledHeaderNav.defaultProps = {};
